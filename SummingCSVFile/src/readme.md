# Summary

This will be the practice file for revisiting COBOL

This program reads a file, processes the CSV line by line, and creates a sum of the values in each line and outputs each line to the terminal.
